# Agent Fabric 0.1.0-beta.1 for macOS

## Artifact

- Channel: direct website download
- Platform: macOS Apple Silicon (`arm64`)
- Bundle ID: `ai.agentfabric.desktop`
- File: `Agent-Fabric-0.1.0-beta.1-arm64.dmg`
- Size: 235,249,189 bytes
- SHA-256: `d68f21b8a1949d8440eba2647a8580528f3283701f4c6649087d307ed97a6d84`

## Verified release state

- The packaged App uses the Agent Fabric robot icon and reports version `0.1.0-beta.1`.
- The App and DMG are signed with Developer ID Application; the App has Hardened Runtime enabled.
- Apple notarization returned `Accepted`; the DMG carries a validated stapled ticket.
- Gatekeeper accepted both the install artifact and the copied App.
- A copy of the stapled DMG was marked with `com.apple.quarantine`, mounted from an isolated download directory and copied out before first launch.
- The quarantined first launch completed through macOS Launch Services, rendered the Account/Agents product and produced no white screen or legacy Personal Agent surface.
- The packaged preload and local MCP smoke passed; MCP exposes exactly `list_agents`, `find_agent`, `ask_agent`, `get_task`.
- The arm64 package contains only the `@openai/codex-darwin-arm64` runtime binary; Intel Mac, Linux and Windows Codex binaries are excluded and rejected by packaged-product verification.

## Install

1. Download `Agent-Fabric-0.1.0-beta.1-arm64.dmg` from the official Agent Fabric download page.
2. Open the DMG and copy Agent Fabric to Applications.
3. Open Agent Fabric and complete Google login in the system browser.

Do not bypass Gatekeeper or remove quarantine attributes. A genuine build is notarized and opens normally under macOS security checks.
